// src/node_modules/@sudoku/stores/highlight.js
// 棋盘高亮状态 store — AI 解释策略时高亮关联格

import { writable } from 'svelte/store';

/** @type {import('svelte/store').Writable<{target: {row:number,col:number}, related: {row:number,col:number}[], excluded: {row:number,col:number}[]} | null>} */
export const highlight = writable(null);

export function clearHighlight() {
  highlight.set(null);
}
