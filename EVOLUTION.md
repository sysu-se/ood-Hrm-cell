# EVOLUTION.md - Homework 2 交互功能设计演进与重构

本次作业旨在 Homework 1.1 版本的基础上，为数独游戏增加“提示（Hint）”与“探索（Explore）”两项高级交互功能。在设计过程中，我们首先基于Hw1.1的homeword review，对领域模型进行了有针对性的重构与演进，将核心业务逻辑与状态管理能力下沉至 `Sudoku` 和 `Game` 对象，同时优化了前端 Store 的职责划分。本文档详述了实现这两项功能的具体设计思路、对原有架构的修改，以及对设计模式演进过程中的思考与反思。

## 1. Hw1.1版本回顾

Homework 1.1 成功构建了数独游戏的基础领域模型，包括 `Sudoku`（核心棋盘逻辑）和 `Game`（会话与历史状态管理）。然而，当引入“提示”和“探索”等更复杂的交互模式时，现有架构暴露出不足，主要体现在：

* **职责划分模糊**：UI 层直接调用外部求解器获取提示，破坏了领域对象的封装性。
* **状态管理不一致**：前端 Store（如 `userGrid`）与领域对象（`Game` 的快照）之间存在数据同步延迟，导致 UI 状态与真实游戏状态脱节。
* **领域模型扩展性受限**：原有的 `guess` 方法过于严格，不允许试错，与“探索模式”的要求相悖。

本次作业的目标是在 Homework 1.1 的基础上，通过面向对象的重构，实现提示与探索功能，并确保代码的可维护性、可扩展性及符合软件工程的良好实践。

## 2. 功能实现与设计决策

### 2.1. 提示功能 (Hint) 的设计与实现

#### 2.1.1. 实现方式

提示功能被细分为两类，并明确了其实现载体：

* **候选数提示 (`getCandidates`)**:
  * **实现载体**: `Sudoku` 对象。
  * **机制**: 遍历 `1` 到 `9`，利用 `Sudoku.isValid(row, col, value)` 方法，筛选出当前空格 `(row, col)` 的所有合法填充值。
* **下一步提示 (`getNextHint`)**:
  * **实现载体**: `Sudoku` 对象。
  * **机制**: 遍历整个棋盘，调用 `getCandidates`。若发现某空格存在唯一合法候选数，则返回该空格坐标与数值。若全盘无唯一解，则返回 `null`，暗示需要进入探索模式。

#### 2.1.2. 领域归属与职责划分

* **归属**: 提示功能（包括候选数计算和下一步推断）在逻辑上**归属于 `Sudoku` 对象**。这是因为这些操作仅依赖于当前棋盘的静态状态和数独规则，不涉及游戏历史、用户会话等会话状态。
* **`Game` 的角色**: `Game` 对象作为整个游戏会话的门面，负责代理 `Sudoku` 对象的提示接口。UI 层通过 `Game.getNextHint()` 调用，`Game` 内部再向其当前管理的 `Sudoku` 快照转发该请求。此设计严格遵循了单一职责原则 ，保证了领域对象与会话管理器的清晰界限。

### 2.2. 探索模式 (Explore) 的设计与实现

#### 2.2.1. 探索模式的本质：状态管理与历史回溯

在设计探索模式时，思考是创建临时子会话，还是引入复杂的树状分支结构？考虑到作业的复杂度要求及代码的可维护性，选择了一种更为精简且高效的方案：**基于快照存档点的“带死胡同记忆的线性历史栈”**。

* **核心状态**:
  * `_isExploring` (boolean): 标记当前是否处于探索模式。
  * `_exploreStartPoint` (number): 记录用户进入探索模式时的 `_currentIndex`，作为回溯的存档点 。
  * `_deadEnds` (set `<string>`): 一个哈希集合，用于存储已知的、会导致冲突的“死胡同”路径。
* **实现机制**:
  * **开始探索**: 调用 `Game.startExplore()`，设置 `_isExploring = true`，并记录 `_exploreStartPoint`。
  * **填数(`guess`)**: 在执行填数前，首先检查该选择是否位于 `_deadEnds` 集合中。若为死胡同，则直接拒绝。否则，正常执行填数，并在必要时截断后续历史。
  * **回溯 (`rollbackExplore`)**: 当用户放弃探索时，首先识别出从存档点到当前第一步试错的“错误决定”。该“错误决定”由“盘面状态哈希”与“填数信息”组合而成。将其加入 `_deadEnds`，然后将 `_currentIndex` 重置回 `_exploreStartPoint`，并截断历史栈。
  * **提交 (`commitExplore`)**: 若探索结果被用户接受，则仅需清除 `_isExploring` 和 `_exploreStartPoint` 标记。当前的 `_history` 栈即成为新的主历史。

#### 2.2.2. 主局面与探索局面的关系

* **对象关系**: 探索模式下的操作对象是当前 `Game` 实例的 `Sudoku` 快照（通过 `clone()` 创建），而非直接修改主历史。
* **状态管理**: 探索模式下的填数操作会产生新的 `Sudoku` 快照，并被推入 `_history` 栈。但其“回滚”行为是通过调整 `_currentIndex` 和截断 `_history` 数组实现，而非创建并合并独立的子分支。
* **深拷贝问题**: `Sudoku` 的 `clone()` 方法确保了每次操作都产生独立的棋盘状态，避免了深拷贝问题。

#### 2.2.3. History 演进

* **数据结构**: 历史记录 `_history` 仍然保持线性栈（Array）的结构。
* **行为变化**:
  * `undo()` 和 `canUndo()` 方法在探索模式下被修改，使其回溯的最大范围限制在 `_exploreStartPoint`。
  * `redo()` 和 `canRedo()` 行为不变。
  * `guess()` 方法在探索模式下，若遇到“死胡同”，会被 `_deadEnds` 拦截，不产生新的历史。

## 3. 代码重构与设计模式应用

### 3.1. 核心代码重构

在实现新功能的过程中，我们对 Homework 1.1 的代码进行了以下关键重构：

1. **修复“单一事实来源”问题**:
   * **位置**: `src/node_modules/@sudoku/stores/grid.js`
   * **修改**: 移除了前端 `userGrid` 的直接写入逻辑。现在的 `userGrid.set()` 只发送指令给 `grid.makeMove()`。`grid` Store 负责调用 `Game`，`Game` 成功更新后，`grid` Store 才将新的 `Sudoku` 快照同步给 UI（通过 `subscribe`）。这确保了领域模型是所有状态的唯一来源。
2. **明确业务语义**:
   * **位置**: `src/components/Board/index.svelte`
   * **修改**: 移除了基于 `$grid[y][x] === 0` 的脆弱业务逻辑判断。改用 `grid.isGiven(x, y)` 和 `$invalidCells` 提供的领域层验证结果，来准确渲染“固定格”、“用户输入”和“冲突格”。
3. **解除领域层过度限制**:
   * **位置**: `src/domain/Sudoku.js`
   * **修改**: 删除了 `guess()` 方法中对 `isValid()` 的强制检查。允许玩家填入冲突数字，以便探索模式能够正常工作并触发死胡同机制。冲突提示的功能现在完全由 UI 层的 `invalidCells` 派生 Store 负责。
4. **封装与代理**:
   * **位置**: `src/domain/Game.js`
   * **修改**: `getSudoku()` 方法返回 `clone()` 后的对象，保护内部历史快照。新增了 `startExplore`, `commitExplore`, `rollbackExplore`, `isDeadEnd` 等方法。`toJSON`/`fromJSON` 方法也增加了对探索模式状态（`_isExploring`, `_exploreStartPoint`, `_deadEnds`）的序列化和反序列化支持。
5. **Store 职责优化**:
   * **位置**: `src/node_modules/@sudoku/stores/grid.js`
   * **修改**: 增加了 `exploreStore` 来响应 `Game` 的探索状态变化。暴露了 `getCandidates()`, `applyHint()`, `startExplore()`, `commitExplore()`, `rollbackExplore()` 等接口，供 UI 调用。

## 4. 设计演进反思

### 4.1. 提示功能与 `Sudoku`/`Game` 的职责划分

之前，提示是通过外部工具（`solveSudoku`）完成的，这显然是表现层与外部服务过度耦合。将其下沉到 `Sudoku` 中，不仅保证了提示逻辑的领域纯净性，也为 `Game` 作为状态管理器提供了清晰的委托路径。

### 4.2. 探索模式的设计演进

探索模式的设计过程是本次作业的核心挑战。从对“分支”与“状态切换”的初步设想，最终收敛为“带记忆的线性栈”模型。这种设计在保证了核心功能（回溯、记忆）的同时，极大地简化了状态管理和 UI 渲染的复杂度。其关键优势在于：

* **状态管理**: 避免了引入复杂的副作用（如 Git 的 DAG 合并），所有历史状态依然存在于一个线性数组中，仅通过指针和标记进行逻辑上的区分。
* **代码简洁性**: `Game` 对象的核心逻辑（如 `guess`, `undo`, `redo`）在加入探索模式后，仅需少量修改，即可正确处理不同状态下的边界条件。
* **记忆机制的优化**: 通过对“错误决定”（盘面状态 + 具体填数操作）进行哈希存储，实现了对失败路径的高效记忆，防止重复试错。
