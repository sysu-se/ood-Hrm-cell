# AGENTIC.md — AI Sudoku 项目 Agentic Development 总结

## 1. 你选择了哪些 Coding Agent？

我选择了 **Claude Code（Anthropic）** 作为唯一的 Coding Agent 完成本次作业的全部开发工作。

选择理由：
- 对现有代码库的理解能力强，能准确分析 Sudoku/Game 的领域模型
- 支持 Plan → Implement → Verify 的工作流闭环
- 能够在单一会话中保持上下文连贯性，贯穿 11 个 WU
- 对 Svelte + Tailwind + Rollup 技术栈有较好支持

---

## 2. 你如何 Harness 项目？

Harness 的核心手段是两个驾驭文件 + 严格的架构约束：

**AGENTS.md** 定义了：
- Agent 的角色和行为准则
- 不可修改的架构边界（Sudoku.js / Game.js / index.js 公共接口锁定）
- 工作流规范（计划 → 实现 → 验证 三步闭环）
- 代码规范（每个 WU ≤ 100 行、修改前列出文件清单）
- 禁止行为清单（不调用真实 API、不引入外部依赖、不修改 Store 层的领域逻辑等）
- 策略引擎架构约束：纯本地规则引擎 + Mock LLM 模板生成

**PLAN.md** 将整个 AI Tutor 系统拆分为 11 个独立 WU，每个 WU 包含：
- 编号和标题
- 明确目标
- 涉及文件清单（新建/修改/不改）
- 可验证的验收标准
- 预估代码行数

此外，在 Harness 过程中持续执行的约束：
- 每个 WU 开始前声明计划 → 等待确认 → 执行 → 验证 → 汇报
- 不夹带计划外的修改
- 每个 WU 有独立的测试验证

---

## 3. 你如何组织 Riding 过程？

Riding 过程遵循 **计划驱动 + 增量交付** 的模式：

1. **架构设计阶段**：在写任何代码之前，先基于"AI 是可对话的伙伴"理念，提出 3 种 AI 能力设计方案，经讨论后选定"AI 导师"方案
2. **Harness 建立阶段**：生成 AGENTS.md 和 PLAN.md，建立项目驾驭框架
3. **分阶段执行**：
   - 阶段一（WU-01~06）：策略引擎核心 — 先骨架，再逐个策略，最后集成
   - 阶段二（WU-07）：AI 解释服务 — Mock 模板生成
   - 阶段三（WU-08~11）：UI 集成 — 先静态 UI，再连引擎，再加高亮，最后 App 集成
4. **遇到问题时的处理**：
   - 策略逻辑 bug（HiddenSingle 的 validCells 条件错误）→ 诊断 → 修复 → 重测
   - 坐标显示不一致（+1 问题）→ 统一 0-based → 全局修复
   - 构建环境问题（PostCSS/Tailwind 版本冲突）→ 诊断依赖树 → 升级修复

---

## 4. 你如何控制 Agent 偏航？

Agent 偏航控制依赖多层机制：

1. **架构约束前置**：AGENTS.md 明确列出禁止修改的文件和接口，Agent 在每次修改前都会检查是否越界
2. **WU 粒度控制**：每个 WU ≤ 100 行，粒度小到可以在一轮对话中完成和验证，偏航风险被控制在单个 WU 内
3. **计划 → 确认 → 执行 流程**：每个 WU 开始前 Agent 必须声明计划并等待确认，避免了 Agent 自主扩大范围
4. **验收环节的即时反馈**：每个 WU 完成后立即运行测试/构建验证，偏航在第一时间被发现
5. **具体偏航事件**：
   - WU-02 NakedSingle 行数略超 40 行（实为 65 行）→ 主动汇报，用户确认接受
   - WU-04 NakedPair 行数超 100 行（实为 120 行）→ 主动汇报
   - HiddenSingle 测试盘面不合法 → Agent 主动诊断找到逻辑 bug 而非盲目修改测试
   - 构建兼容性修复 → Agent 诊断出 postcss-clean 是根因而非盲目升级所有包

---

## 5. 你如何进行验收？

每个 WU 的三步验收流程：

1. **语法检查**：`node -c` 或 Svelte 编译器验证新文件无语法错误
2. **单元/集成测试**：每个策略引擎文件有其独立的测试用例（构造特定盘面 → 调用 detect → 验证 StrategyResult 各字段）
3. **构建验证**：`npm run build` 确保整个项目可编译

最终 WU-11 进行了 **21 项端到端验证**，覆盖：
- 4 种策略检测 + 值验证
- AI 解释生成（含坐标 0-based 验证）
- 高亮映射
- Sudoku 公共接口完整性
- Fallback 兜底逻辑

---

## 6. AI Sudoku 的 AI 能力是什么？

本项目的 AI 能力定位为 **AI 导师（AI Tutor）**，核心能力：

- **策略推理**：检测 4 种标准数独策略（唯余法、摒除法、显性数对、区块排除），给出结构化的推理链条
- **自然语言解释**：将策略引擎的结构化输出翻译为中文自然语言，命名策略、引用关联格坐标、给出结论
- **对话交互**：通过 ChatPanel，用户可以用自然语言提问（"解释 (0,2)"、"下一步该看哪里？"、"候选数 (3,5)"）
- **棋盘可视化**：AI 解释时自动高亮关联格（目标格绿色、关联格蓝色、排除格红色淡化）

技术实现采用 **纯本地规则引擎 + Mock LLM 模板** 的 Hybrid 架构：
- 策略引擎：100% 确定性规则，离线可用，可单元测试
- AI 解释：高质量预定义模板，不依赖外部 API（可随时替换为真实 LLM 调用）

---

## 7. 你认为哪些部分最适合 Agent 完成？

1. **规则引擎实现**：策略检测算法有明确的输入/输出契约（输入 Sudoku + 坐标 → 输出 StrategyResult），Agent 能够根据策略定义精确实现
2. **模板化文本生成**：每种策略对应固定的解释模板，Agent 可以生成自然、教育性的中文文本
3. **UI 组件搭建**：ChatPanel 的 Svelte 组件结构、样式、事件处理是标准的 UI 开发模式，Agent 掌握良好
4. **依赖问题诊断**：PostCSS/Tailwind 版本冲突的根因分析和修复方案提出，Agent 的依赖树分析能力表现出色
5. **测试用例编写**：为每种策略构造测试盘面、验证 StrategyResult 各字段

---

## 8. 你认为哪些部分仍然需要人类主导？

1. **产品方向决策**：AI 能力的设计方案选择（导师 vs 对弈伙伴 vs 复盘教练）、交互模式定义，需要人类理解用户需求和体验目标
2. **架构设计**：策略引擎的接口设计、Mock 优先的架构决策、WU 拆分粒度控制，这些需要经验判断
3. **验收标准的制定**：什么算"通过"、什么算"可接受的行数超标"、什么时候需要停下来修复 vs 继续前进
4. **测试数据构造**：为策略引擎构造合法且有效的测试盘面需要领域理解（如构造 Hidden Single 的正确测试用例）
5. **风格和一致性判断**：坐标应该 0-based 还是 1-based 是一个 UX 决策，Agent 默认加了 `+1`，人类纠正为 0-based
6. **驾驭文件的设计**：AGENTS.md 和 PLAN.md 本身需要人类来写，因为这些文件定义了 Agent 的行为边界

---

## 9. 你认为本次 AD 中最大的困难是什么？

**最大的困难是构建环境兼容性问题的诊断和修复**。

具体表现为 PostCSS/Tailwind 版本冲突导致 `npm run build` 失败。这个问题需要：
1. 理解 PostCSS 7 vs 8 的 API 差异（`node.getIterator` 的移除）
2. 分析完整的依赖树（`tailwindcss@1.9.6` → PostCSS 7 子依赖 vs `postcss@8.4.31` 顶层）
3. 定位真正的罪魁祸首（`postcss-clean@1.1.0` 内部使用 PostCSS 6，在 PostCSS 8 管道中产生不兼容节点）
4. 选择正确的修复方案（升级 tailwindcss → v2.2.19，替换 postcss-clean → cssnano，补回 shadow-outline）

这不是一个"写代码"的问题，而是一个"理解复杂工具链"的问题。Agent 在这一过程中表现出了较强的诊断能力，但最终的决策和验证仍然需要人类参与。

次要困难：
- **坐标系统一致性**：领域层使用 0-based，Agent 在模板中默认加了 +1，需要人类纠正
- **测试盘面构造**：Agent 构造的第一个 Hidden Single 测试是不合法的（同行有重复数字），第二个来自用户的测试也是不成立的（目标格不是 Hidden Single），直到第三个才正确

---

## 10. 如果继续迭代，你会如何改进你的 AD 方法？

1. **建立项目级测试基础设施**：在 WU-01 就先建立一个测试 runner，每个 WU 的验收测试沉淀为可复用的测试用例，而非一次性脚本
2. **引入 Contract Testing**：为 StrategyEngine 和 AIExplainService 定义正式的接口契约，确保后续替换为真实 LLM 时行为一致
3. **WU 的并行化**：WU-02~05（四个策略实现）和 WU-08（Chat UI 壳）是互不依赖的，可以在同一会话中并行推进（多 Agent），缩短总开发时间
4. **Harness 文件的版本化**：AGENTS.md 和 PLAN.md 应该随项目演进更新（例如 WU 完成后在 PLAN.md 中标记完成状态）
5. **Prompt 模板积累**：将本次有效的 Prompt 模式（如"先列出文件清单再执行"、"每 WU 三步闭环"）沉淀为可复用的 Prompt 模板
6. **更早的端到端集成**：WU-11 才做 App 集成略晚，建议在 WU-07（AIExplainService）之后就做一个最小可交互的集成（smoke test），更早发现集成问题
7. **引入代码审查 Agent**：在人类验收之前，先用另一个 Agent 对每个 WU 的产出做自动化代码审查（安全检查、风格检查、覆盖率检查）
